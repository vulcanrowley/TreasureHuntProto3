export default class Dungeon extends Phaser.Scene {
  constructor() {
    super({ key: 'DungeonScene' })
    
  }

  preload() {// layout based on DungeonCode
  }

  create() {
  }

  update() {
  }
}